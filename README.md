# Warning: This is WIP

To update all dependencies in your project to their latest versions, navigate to your project's root directory and run the command npx npm-check-updates -u, followed by npm install to install the updated packages. This will ensure that your package.json reflects the latest versions of your dependencies.

npm install --save @resolid/keyv-sqlite --legacy-peer-deps

