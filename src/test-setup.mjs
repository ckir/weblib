import '@dotenvx/dotenvx/config';
// import path from 'path';

// Load environment variables from a .env file in the project's root directory.
// We resolve the path from the current working directory where the 'jest' command is run.
// const result = config({ path: path.resolve(process.cwd(), '.env') });

// if (result.error) {
//   // This allows tests to run in a CI environment where variables are set differently.
//   console.warn('Could not find .env file. Relying on environment variables set externally.');
// }